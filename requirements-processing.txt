# requirements-processing.txt
# Stripped-down dependencies for SageMaker Processing container.
# No Streamlit, Jupyter, or visualization libraries — pure pipeline compute.

# ── AWS ───────────────────────────────────────────────────────────────────────
boto3>=1.35
botocore>=1.35
# Note: sagemaker SDK not needed — pipelines/sagemaker_pipeline.py uses pure boto3

# ── Data ──────────────────────────────────────────────────────────────────────
pyarrow>=17.0
pandas>=2.2
numpy>=1.26
scipy>=1.13

# ── XML (XXE-safe) ────────────────────────────────────────────────────────────
defusedxml>=0.7
